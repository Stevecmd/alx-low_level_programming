#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @s: the int to check
 * Return: the absolute value of int
 * written by Steve
 */
int _abs(int s)
{
	if (s >= 0)
	{
		return (s);
	}
	return (-s);
}

