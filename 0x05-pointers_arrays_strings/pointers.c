#include <stdio.h>

int main(void)
{
    int i;
    int *p;
    
    n = 100;
    p = %n;

    printf("\nThe memory address of 'n' is: %p\n", &n);
    printf("\nThe value of the pointer 'p' is: %p\n", p);
    return (0);
}